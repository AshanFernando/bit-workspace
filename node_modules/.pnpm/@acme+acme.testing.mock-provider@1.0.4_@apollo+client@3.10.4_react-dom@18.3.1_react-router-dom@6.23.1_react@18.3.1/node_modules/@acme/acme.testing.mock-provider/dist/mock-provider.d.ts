import type { ReactNode } from 'react';
export type MockProviderProps = {
    /**
     * sets the component children.
     */
    children?: ReactNode;
    /**
     * do not use the memory router.
     */
    noRouter?: boolean;
    /**
     * do not use the theme.
     */
    noTheme?: boolean;
};
/**
 * a mock provider for testing and previewing components
 * in the Acme platform.
 */
export declare function MockProvider({ children, noRouter, noTheme }: MockProviderProps): import("react/jsx-runtime").JSX.Element;
