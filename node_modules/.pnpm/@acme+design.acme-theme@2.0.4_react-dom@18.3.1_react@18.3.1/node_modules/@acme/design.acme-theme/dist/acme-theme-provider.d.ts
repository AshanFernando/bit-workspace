/**
 * creating and declaring the acme theme.
 * define the theme schema as a type variable for proper type completions.
 */
export declare const AcmeThemeProvider: import("@bitdesign/sparks.sparks-theme").SparkThemeType<{
    backgroundColor: string;
    textColor: string;
    primaryColor: string;
    borderColor: string;
    surfaceColor: string;
}>;
/**
 * a react hook for contextual access to design token
 * from components.
 */
export declare const useTheme: () => {
    backgroundColor: string;
    textColor: string;
    primaryColor: string;
    borderColor: string;
    surfaceColor: string;
};
