export class Discussion {
    constructor(
    /**
     * discussion title.
     */
    title, 
    /**
     * name of the user.
     */
    username) {
        this.title = title;
        this.username = username;
    }
    /**
     * serialize a Discussion into
     * a serializable object.
     */
    toObject() {
        return {
            title: this.title,
            username: this.username
        };
    }
    /**
     * create a Discussion object from a
     * plain object.
     */
    static from(plainDiscussion) {
        return new Discussion(plainDiscussion.title, plainDiscussion.username);
    }
}
//# sourceMappingURL=discussion.js.map