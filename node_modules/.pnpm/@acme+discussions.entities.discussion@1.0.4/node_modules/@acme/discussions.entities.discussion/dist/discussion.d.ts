export type PlainDiscussion = {
    /**
     * discussion title.
     */
    title: string;
    /**
     * name of the user.
     */
    username: string;
};
export declare class Discussion {
    /**
     * discussion title.
     */
    readonly title: string;
    /**
     * name of the user.
     */
    readonly username: string;
    constructor(
    /**
     * discussion title.
     */
    title: string, 
    /**
     * name of the user.
     */
    username: string);
    /**
     * serialize a Discussion into
     * a serializable object.
     */
    toObject(): {
        title: string;
        username: string;
    };
    /**
     * create a Discussion object from a
     * plain object.
     */
    static from(plainDiscussion: PlainDiscussion): Discussion;
}
