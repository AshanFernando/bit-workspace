import type { K8Adapter } from '@backend/kubernetes.kubernetes-deployer';
type GKEAdapterOptions = {
    /**
     * The name (project-relative) of the Google Compute Engine zone in which the cluster resides,
     * such as `us-central1-a`.
     */
    zone: string;
    /**
     * The name of the cluster.
     */
    clusterName: string;
    /**
     * The JSON key of the service account to use to authenticate with GCP. (Stringified JSON object)
     */
    keyJson: string;
};
export declare class GKEAdapter implements K8Adapter {
    private gcloud;
    private keyPath;
    private projectId;
    private accessToken;
    constructor(gcloud: GKEAdapterOptions);
    getCredentials(): Promise<{
        endpoint: string;
        certificate: string;
        token: string;
    }>;
    private formatKey;
    private authGcloud;
    private isPath;
    private parse;
}
export {};
