interface IRenderProps {
    path: string;
    cookie: string;
    req: Request;
    res: Response;
}
export declare const render: ({ cookie, req, res }: IRenderProps) => Promise<{
    script: string;
    html: string;
}>;
export {};
/**
 * implement loadScripts() to inject scripts to the head
 * during SSR.
 */
