import { gql } from "graphql-tag";
export function discussionServerSchema(discussionServer) {
    const typeDefs = gql `#graphql

  type Discussion {
    title: String
    username: String
  }

  type Query {
    listDiscussions: [Discussion]
  }
`;
    const resolvers = {
        Query: {
            listDiscussions: async () => {
                const discussions = await discussionServer.listDiscussions();
                return discussions.map((discussion) => {
                    return discussion.toObject();
                });
            },
        },
    };
    return {
        typeDefs,
        resolvers
    };
}
//# sourceMappingURL=discussion-server.graphql.js.map