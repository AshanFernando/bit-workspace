import { ApplicationInstance, AppBuildContext, AppBuildResult, AppDeployContext } from "@teambit/application";
import { GatewayContext, GatewayServer } from "./gateway-server.js";
export type StartGatewayOptions = {
    /**
     * port the range.
     */
    port?: number;
    /**
     * X-Auth
     */
    platformHeaders?: Record<string, string>;
};
export declare class DefaultGateway implements GatewayServer {
    name: string;
    /**
     * run the symphony gateway.
     */
    run(context: GatewayContext): Promise<ApplicationInstance>;
    build(context: AppBuildContext): Promise<AppBuildResult>;
    deploy(context: AppDeployContext): Promise<void>;
}
