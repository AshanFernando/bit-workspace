import { User } from "@bitdev/node.examples.user";
/**
 * user server
 */
export declare class UserServer {
    /**
     * list users.
     */
    listUsers(): Promise<User[]>;
    /**
     * create a new instance of a user server.
     */
    static from(): UserServer;
}
