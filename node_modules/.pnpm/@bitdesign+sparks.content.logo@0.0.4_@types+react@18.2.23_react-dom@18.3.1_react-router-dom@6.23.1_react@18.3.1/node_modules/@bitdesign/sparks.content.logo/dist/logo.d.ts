import type { ReactNode } from 'react';
export type LogoProps = {
    /**
     * children to render below the logo.
     */
    children?: ReactNode;
    /**
     * name of the logo.
     */
    displayText?: boolean;
    /**
     * custom size of the logo.
     */
    imageSize?: number;
    /**
     * name of the logo.
     */
    name?: string;
    /**
     * slogan for the logo.
     */
    slogan?: string;
    /**
     * source for the logo.
     */
    src?: string;
    /**
     * set the logo as a link.
     */
    href?: string;
    /**
     * logo text class.
     */
    logoTextClass?: string;
    /**
     * class name to use.
     */
    className?: string;
    /**
     * alt text for the logo.
     */
    alt?: string;
};
export declare function Logo({ children, alt, logoTextClass, href, src, displayText, name, className, slogan, imageSize }: LogoProps): import("react/jsx-runtime").JSX.Element;
