import { ColorOptions } from './color.js';
export type GenerateColorSchemeOptions = {
    factor: number;
};
/**
 * compute a gradient from a user color options.
 */
export declare function computeGradient(baseColor: ColorOptions): string[];
