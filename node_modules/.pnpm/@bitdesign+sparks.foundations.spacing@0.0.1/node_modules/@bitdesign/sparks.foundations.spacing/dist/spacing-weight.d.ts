export type SpacingWeightOptions = {
    segment: string;
    /**
     * weight of the spacing.
     * scales by the number of levels by the multiplier
     */
    level: number;
    /**
     * unit multiplier.
     */
    multiplier: number;
    /**
     * base unit.
     */
    baseUnit: number;
};
/**
 * spacing weight is a level in the spacing scheme
 */
export declare class SpacingWeight {
    /**
     * options for computing the spacing weight.
     */
    private options;
    constructor(
    /**
     * options for computing the spacing weight.
     */
    options: SpacingWeightOptions);
    get segment(): string;
    /**
     * level for the spacing weight.
     */
    get level(): number;
    get multiplier(): number;
    /**
     * weight for the spacing.
     */
    get weight(): number;
    /**
     * token name for spacing.
     */
    get token(): string;
    /**
     * size computed for the level in rem.
     */
    get rem(): string;
    get px(): string;
    /**
     * pixels computed for the level.
     */
    get pixels(): number;
    toObject(): {
        pixels: number;
        rem: string;
        token: string;
        multiplier: number;
        weight: number;
    };
    toToken(): {
        [x: string]: string;
    };
    /**
     * create a spacing weight.
     */
    static create(options: SpacingWeightOptions): SpacingWeight;
}
