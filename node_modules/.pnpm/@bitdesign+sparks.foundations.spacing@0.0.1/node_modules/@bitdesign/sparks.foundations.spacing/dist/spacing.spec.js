import { Spacing } from './spacing.js';
describe('default spacing', () => {
    const spacing = Spacing.create();
    it('should include levels', () => {
        expect(spacing.weights.length).toEqual(13);
    });
    it('should include a 700 space weight with 56px', () => {
        const weight = spacing.get(700);
        expect(weight.pixels).toEqual(56);
    });
    it('should include a 300 weight with 24px', () => {
        const weight = spacing.get(300);
        expect(weight.pixels).toEqual(24);
    });
    it('should include rem', () => {
        const weight = spacing.get(500);
        expect(weight.rem).toEqual('2.52rem');
    });
    it('should be of segment `small`', () => {
        const weight = spacing.get(100);
        expect(weight.segment).toEqual('small');
    });
    it('should be of segment `large`', () => {
        const weight = spacing.get(400);
        expect(weight.segment).toEqual('large');
    });
});
//# sourceMappingURL=spacing.spec.js.map