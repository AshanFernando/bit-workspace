import type { ComponentType, ReactNode } from 'react';
import { type FlexProps } from '@bitdesign/sparks.layout.flex';
export type HeaderProps = {
    /**
     * set the children for the header.
     * used in the center of the layout.
     */
    children?: ReactNode;
    /**
     * logo to use.
     */
    logo?: ReactNode;
    /**
     * badges to include at the left of the header.
     */
    actions?: ComponentType[];
    /**
     * class name for the header.
     */
    className?: string;
    /**
     * class name for the content container.
     */
    contentClassName?: string;
    /**
     * class name to use for logo container.
     */
    logoClassName?: string;
    /**
     * class name to use for badges.
     */
    actionsClassName?: string;
} & FlexProps;
export declare function Header({ children, actions, logo, contentClassName, logoClassName, actionsClassName, className, ...rest }: HeaderProps): import("react/jsx-runtime").JSX.Element;
