import type { ReactNode } from 'react';
import { RouterContextType } from '@teambit/base-react.navigation.link';
export type NavigationProviderProps = {
    /**
     * children of the provider.
     */
    children: ReactNode;
    /**
     * implementation for the navigation provider.
     * defaults to use `react-router`.
     * for other adapters see: https://bit.cloud/frontend
     */
    adapter?: RouterContextType;
};
export declare function NavigationProvider({ children, adapter, }: NavigationProviderProps): import("react/jsx-runtime").JSX.Element;
