import { UseLocation, UseNavigate } from "@teambit/base-react.navigation.link";
import { ComponentType } from "react";
import { LinkProps } from "./link.js";
/**
 * Link type.
 */
export declare type LinkType = ComponentType<LinkProps>;
export interface NavigationAdapter {
    /**
     * implement a link for router system.
     *
     * @example
     * import { Link } from 'next/link';
     * return <Link />
     */
    Link?: LinkType;
    /**
     * use location implementation for
     * obtaining the browser location.
     */
    useLocation?: UseLocation;
    /**
     * obtain a programmatic navigate function.
     */
    useNavigate?: UseNavigate;
}
