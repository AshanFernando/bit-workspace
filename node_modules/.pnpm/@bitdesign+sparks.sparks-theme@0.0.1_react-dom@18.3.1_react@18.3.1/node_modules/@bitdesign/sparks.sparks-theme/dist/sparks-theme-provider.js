import { createTheme } from '@teambit/base-react.themes.theme-provider';
import { sparksThemeTokens } from './sparks-theme-tokens.js';
/**
 * creating and declaring the sparks-theme theme.
 * define the theme schema as a type variable for proper type completions.
 */
export const SparksThemeProvider = createTheme({
    theme: sparksThemeTokens,
});
/**
 * a react hook for contextual access to design token
 * from components.
 */
export const { useTheme } = SparksThemeProvider;
//# sourceMappingURL=sparks-theme-provider.js.map