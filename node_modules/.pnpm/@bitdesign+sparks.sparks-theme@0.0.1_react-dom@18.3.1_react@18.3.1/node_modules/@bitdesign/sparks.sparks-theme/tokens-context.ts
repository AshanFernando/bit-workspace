import { Spacing } from "@bitdesign/sparks.foundations.spacing";
import { ColorPalette } from "@bitdesign/sparks.foundations.color-palette";

export type LoadTokensFn<T> = (context: LoadTokensContext) => T;

export class LoadTokensContext {
  constructor(
    /**
     * color palette of the theme.
     */
    readonly colorPalette: ColorPalette,

    /**
     * spacing for the theme.
     */
    readonly spacing: Spacing
  ) {}
}
