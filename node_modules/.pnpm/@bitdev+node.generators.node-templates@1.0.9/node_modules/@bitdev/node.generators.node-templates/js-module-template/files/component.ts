import { ComponentContext } from '@teambit/generator';

export const componentFile = (context: ComponentContext) => {
  const { name, namePascalCase: Name, nameCamelCase } = context;
  return {
    relativePath: `${name}.js`,
    content: `export function ${nameCamelCase}() {
  return "hello world";
}
`,
  };
};
