export type GenerateTemplateOptions = {
    publicDir: string;
    publicPath: string;
    indexHtmlPath: string;
};
export declare function generateHtmlTemplate(options: GenerateTemplateOptions): Promise<string>;
