export declare class AssetManifest {
    private assetManifest;
    constructor(assetManifest: Record<string, string>);
    getStylesheets(): string[];
    getScripts(): string[];
    getStylesheetsString(publicPath?: string): string;
    getTags(publicPath?: string): {
        scripts: string;
        stylesheets: string;
    };
    static fromPath(path: string, manifestName?: string): Promise<AssetManifest>;
}
