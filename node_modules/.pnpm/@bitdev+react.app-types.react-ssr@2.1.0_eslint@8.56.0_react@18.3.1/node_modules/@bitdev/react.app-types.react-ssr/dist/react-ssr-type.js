import { ReactSsr } from './react-ssr.js';
export class ReactSsrType {
    constructor() {
        this.name = 'react-ssr';
    }
    createApp(options) {
        return new ReactSsr(options.name, options.filename, options.defaultPort, options.publicPath, undefined, options.transformers || [], options.artifactName, options.serverRoot, options.compileOutDir, options.clientRoot, options.deploy);
    }
    static from() {
        return () => {
            return new ReactSsrType();
        };
    }
}
//# sourceMappingURL=react-ssr-type.js.map